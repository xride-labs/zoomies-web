import { BrandPortalLayout } from '@/components/brand/brand-portal-layout'

export default function BrandManagementLayout({ children }: { children: React.ReactNode }) {
  return <BrandPortalLayout>{children}</BrandPortalLayout>
}
